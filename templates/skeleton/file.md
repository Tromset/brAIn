# <file title>

> 🧠 [Hub](README.md) · [Variables](brain.yaml)

<What this file contains, in one or two sentences. Link related sibling files inline, e.g. "see the config in [config.json](config.json)".>

## <section>

Code lives inside the Markdown file, in a fenced block tagged with its language (rule 4):

```python
def hello():
    return "Salut à tous"
```

One file can hold several fences (e.g. the HTML and the CSS of one page).
